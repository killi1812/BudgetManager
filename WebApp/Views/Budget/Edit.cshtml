@model WebApp.ViewModels.BudgetVM

@{
    ViewData["Title"] = "Edit Budget";
}

<h1>Edit Budget</h1>

<form asp-action="Edit" method="post">
    <input type="hidden" asp-for="Guid" />

    <div class="form-group">
        <label asp-for="Sum" class="control-label">Budget Amount</label>
        <input asp-for="Sum" class="form-control" />
        <span asp-validation-for="Sum" class="text-danger"></span>
    </div>

    <div class="form-group">
        @* <label asp-for="UserId" class="control-label">User</label> *@
        @* <select asp-for="UserId" class="form-control" asp-items="@(new SelectList(Model.Users, "UserId", "UserName"))"> *@
        @*     <option value="">-- Select User --</option> *@
        @* </select> *@
        @* <span asp-validation-for="UserId" class="text-danger"></span> *@
    </div>

    <div class="form-group">
        @* <label asp-for="CategoryId" class="control-label">Category</label> *@
        @* <select asp-for="CategoryId" class="form-control" asp-items="@(new SelectList(Model.Categories, "CategoryId", "CategoryName"))"> *@
        @*     <option value="">-- Select Category --</option> *@
        @* </select> *@
        @* <span asp-validation-for="CategoryId" class="text-danger"></span> *@
    </div>

    <div class="form-group">
        <button type="submit" class="btn btn-primary">Save</button>
        <a asp-action="Index" class="btn btn-secondary">Cancel</a>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
